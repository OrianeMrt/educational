<header>
    <nav class="navbar navbar-expand-lg navbar-dark danger-color">
        <a class="navbar-brand" href="{{ path('homepage') }}">Educat - Back-office |</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_users') }}"><i class="fas fa-users"></i> Liste des utilisateurs</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('admin_category') }}"><i class="fas fa-file"></i> Liste des categories</a>
                </li>
            </ul>
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('admin_account_logout') }}">Se déconnecter</a>
                    </li>
                </ul>
        </div>
    </nav>
</header>
