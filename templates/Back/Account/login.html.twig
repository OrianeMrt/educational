<!doctype html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>Connexion back-office</title>
    {% include '_partials/listcss/css.html.twig' %}

    <style>
        body{
            display: flex;
            justify-content: center;
            height: 100vh;
            background-color: rgba(2, 23, 37, 0.4);
        }
        .card{
            margin-top: 20%;
        }
    </style>
</head>
<body>
<div class="col-xl-5">
    <div class="card">
        <div class="card-header danger-color">
            <h4 class="card-title white-text mt-2">
                <i class="fas fa-lock"></i>
                Connexion à l'administration
            </h4>
        </div>
        <div class="card-body">
            {% if hasError %}
                <div class="alert alert-warning">
                    <p class="mt-3 text-center">Les informations que vous avez fournies ne sont pas bonnes.</p>
                </div>
            {% endif %}
            <form method="post">
                <div class="form-group">
                    <label for="_username">Email</label>
                    <input type="text" class="form-control" placeholder="Adresse email.." value="{{ username }}" name="_username">
                </div>
                <div class="form-group">
                    <label for="_password">Mot de passe</label>
                    <input type="password" class="form-control" placeholder="Mot de passe..." name="_password">
                </div>
                <div class="form-group">
                    <button type="submit" class="btn btn-primary">
                        <i class="fas fa-lock-open"></i>
                        Connexion
                    </button>
                    <a href="{{ path('homepage') }}" class="btn btn-link">
                        <i class="fas fa-arrow-circle-left"></i>
                        Retour au site
                    </a>
                </div>
            </form>
        </div>
    </div>
</div>
{% include '_partials/listjs/js.html.twig' %}
</body>
</html>