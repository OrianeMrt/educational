<header>
    <nav class="navbar navbar-expand-lg navbar-dark elegant-color">
        <a class="navbar-brand" href="{{ path('homepage') }}">Educat |</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav mr-auto">

                {% if is_granted('ROLE_USER') %}
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('work.user') }}">Mes Travaux</a>
                    </li>
                    <li class="nav-item">
                    <a class="nav-link" href="{{ path('category_index') }}">Listes des categories</a>
                </li>
                    <li class="nav-item">
                        <a class="nav-link disabled" href="#">Disabled</a>
                    </li>
                {% endif %}
            </ul>
            {% if app.user %}
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('account') }}">Mon compte</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('account_logout') }}">Se déconnecter</a>
                    </li>
                </ul>
            {% else %}
                <ul class="navbar-nav ml-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('account_register') }}">Inscription</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('account_login') }}">Connexion</a>
                    </li>
                </ul>
            {% endif %}

        </div>
    </nav>
</header>
